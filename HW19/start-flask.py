import subprocess, sys

# if len(sys.argv) != 2:
#     print('Limit 2 args passed allowed.')
#     exit()

# filename = sys.argv[1]

#export_file = subprocess.call(['export', f'FLASK_APP=song-server-back.py'])
# export_dev = subprocess.run(['export', 'FLASK_ENV=development'])
# start_flask = subprocess.run(['python3', '-m', 'flask', 'run'])
